name: make test

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build-ubuntu:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: dir
      run: ls -alF
      working-directory: ./src
    - name: make
      run: make
      working-directory: ./src
    - name: make test
      run: make test
      working-directory: ./src
